# Base runtime — always required. Detector model deps are split into
# requirements-<detector>.txt so a YOLO-only deployment doesn't pull torch+transformers.
aiortc==1.9.0
websockets==12.0
numpy==1.26.4
# Headless OpenCV (no GUI libs) — safe on servers/containers. Swap for
# opencv-python if you actually want the local debug window (QVS_ENABLE_DISPLAY=true).
opencv-python-headless==4.10.0.84
